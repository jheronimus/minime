diff --git forkSrcPrefix/Makefile forkDstPrefix/Makefile
index 5f6cabc..de4640a 100644
--- forkSrcPrefix/Makefile
+++ forkDstPrefix/Makefile
@@ -571,6 +571,18 @@ else ifneq (,$(findstring windows_msvc2017,$(platform)))
    LDFLAGS += -DLL

 # Windows
+# Minime
+else ifeq ($(platform), minime)
+   TARGET := $(TARGET_NAME)_libretro.so
+   CC = $(CROSS_COMPILE)gcc
+   CXX = $(CROSS_COMPILE)g++
+   AR = $(CROSS_COMPILE)ar
+   fpic := -fPIC
+   SHARED := -shared -Wl,-version-script=$(LIBRETRO_DIR)/link.T
+   PLATFORM_DEFINES := -DCC_RESAMPLER -DCC_RESAMPLER_NO_HIGHPASS
+   CFLAGS += -fomit-frame-pointer -ffast-math -march=armv8-a+crc
+   CXXFLAGS += $(CFLAGS)
+
 else
 	fpic :=
 	TARGET := $(TARGET_NAME)_libretro.dll
