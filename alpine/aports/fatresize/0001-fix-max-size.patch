--- a/fatresize.c
+++ b/fatresize.c
@@ -604,6 +604,9 @@
 
   if (opts.size == LLONG_MAX) {
     opts.size = constraint->max_size * dev->sector_size;
+    if (opts.size > part_geom.length * dev->sector_size) {
+      opts.size = part_geom.length * dev->sector_size;
+    }
     ped_constraint_destroy(constraint);
   }
 
