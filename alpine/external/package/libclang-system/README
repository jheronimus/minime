host-libclang-system
===================

Symlinks the Debian container's preinstalled libclang libraries into
Buildroot's host directory so that Rust bindgen can find them without
building host-clang from source.

Required container packages: libclang1-22, libclang-cpp22.

Depends on host-llvm-system for the LLVM runtime library.
