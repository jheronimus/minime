name: Build Minime (RK3326)

on:
  workflow_dispatch:

jobs:
  build:
    if: false
    name: Build RK3326 Firmware
    runs-on: self-hosted
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          clean: false

      - name: Verify/Install Dependencies
        run: |
          ./scripts/prepare-linux.sh

      - name: Build Image
        run: |
          echo "Building image for board: rk3326"
          make image BOARD=rk3326
